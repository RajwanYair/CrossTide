/**
 * @crosstide/worker — Cloudflare Worker entry point reference.
 *
 * This package exists to mark the worker as an npm workspace member.
 * The actual worker source lives in the root `worker/` directory until
 * the full migration is complete.
 */
export { default } from "../../worker/index";
