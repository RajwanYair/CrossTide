# analysis_options.yaml — Static analysis configuration for CrossTide.
#
# flutter_lints/flutter.yaml is the recommended baseline.
# Additional rules below tighten code quality for a production Flutter app.
# All generated files (*.g.dart, *.freezed.dart, *.drift.dart) are excluded.

include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    missing_required_param: error
    missing_return: error
    todo: ignore                 # TODOs are tracked in GitHub Issues
  exclude:
    - '**/*.g.dart'
    - '**/*.freezed.dart'
    - '**/*.drift.dart'
    - '**/*.mocks.dart'

linter:
  rules:
    # Style
    prefer_single_quotes: true
    always_declare_return_types: true
    annotate_overrides: true
    sort_constructors_first: true
    directives_ordering: true

    # Correctness
    avoid_empty_else: true
    avoid_print: true
    cancel_subscriptions: true
    close_sinks: true
    unawaited_futures: true
    unnecessary_lambdas: true
    use_rethrow_when_possible: true

    # Performance / const
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true

    # Null safety
    prefer_final_locals: true
    prefer_final_in_for_each: true

    # Domain purity helpers
    avoid_dynamic_calls: true
    avoid_type_to_string: false  # used in log output

    # Pub
    secure_pubspec_urls: true
